[window_defaults]
evict_interval = "1s"
max_window_bytes = "512MB"
max_total_bytes = "2GB"
evict_policy = "time_first"
watermark = "1s"
allowed_lateness = "30m"
late_policy = "drop"

[window.evt_events]
mode = "local"
max_window_bytes = "512MB"
over_cap = "30m"
