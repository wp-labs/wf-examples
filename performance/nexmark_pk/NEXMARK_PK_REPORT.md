# warp-fusion NEXMark 性能 PK 报告：对比 Flink OSS 与 Flink VVR
> **范围**：warp-fusion（wp-reactor 引擎）在 NEXMark 上的吞吐表现，对照阿里云《Nexmark 性能白皮书》发布的 **OSS Flink** 与 **实时计算 Flink（VVR）** 基线。
> **数据截止**：PK 主表采用 **2026-08-27 Linux 100M 权威跑批**（21/21 clean，v2.0.7，哨兵 EPS 口径），与白皮书**同规模（100M 条）**——最对等的对照。q6 已移出 `all` 套件（架构性慢，单跑保留，无白皮书基线）。
---
## 0. 摘要
- **相对 OSS Flink**：20/20 有基线查询 **6.06×–231.49× 全面领先**。
- **相对 VVR**：20/20 有基线查询 **全部达 VVR**，倍数 **1.76×–39.98×**。
- **⚠ 口径声明**：wfusion 与 VVR 使用**相同型号云服务器**，残余不对等仅剩计算资源计量口径（8 核 vs 8CU，OSS 3×12vCPU）；数据规模已对齐 100M，结论作**量级参照**。详见 §3。

## 1. 背景与基准对象
**NEXMark** 是流处理领域社区公认的基准（源自 Apache Calcite / Nexmark，被 Flink 官方 `nexmark` 测试集采纳），由三流事件构成：
| 流 | 占比 | 说明（100M 总量） |
|---|---|---|
| person_events | 2% | 2M person |
| auction_events | 6% | 6M auction |
| bid_events | 92% | 92M bid |

wfusion 生成器 `wfgen gen-nexmark` 按官方默认配置确定性生成（价格对数均匀、hot auction/seller/bidder、事件时间严格递增），**同一 count + seed 字节级确定**，与 Flink 官方定义逐项对齐（见 `docs/NEXMARK_CONFORMANCE.md`）。

查询集 **Q1–Q22 共 22 条**，覆盖无状态投影/过滤、join（TUMBLE/asof/snapshot）、滑窗/跳窗/top-N、会话窗口、stats 聚合等流处理典型形态。阿里白皮书发布其中 **q0–q22（23 条）** 的OSS/VVR 吞吐——本报告对比 q1–q22（q0 为计数投影，白皮书有基线但非 NEXMark 标准查询集，本套件未纳入）。**当前自动化套件 21 条**（Q6 因架构性慢移出 `all`、单跑保留；Q6 亦无白皮书基线）。

## 2. Query Case 覆盖场景与应用范围
NEXMark 的 **Q1–Q22** 是流处理社区定义的 22 条标准查询，覆盖从最简单无状态投影到复杂状态聚合、多流 join、会话窗口、Top-N 的完整原语谱系。wfusion 以 WFL 的 **`match`（序列/状态机检测）** 与 **`stats`（列式统计聚合）** 两类一等原语表达全部查询。下面按「场景类别」归类，并映射到真实 **实时数仓 / ETL、SIEM 安全检测、IoT 监控、风控** 四类应用范围。

### 2.1 场景类别与覆盖 Query
| 场景类别 | 覆盖 Query | 流处理形态 | 典型应用范围 |
|---|---|---|---|
| 无状态投影 / 标量计算 | q1, q2, q3, q14, q22 | 字段变换、货币换算、条件选择、URL 解析 | 事件字段归一化与富化、黑白名单命中、日志标准化（ECS/CEF） |
| 全量落盘 / 热冷分流 | q10, q21 | 每条事件一行输出、热通道映射 + 冷 URL | 原始事件归档、取证留痕、热冷数据分层 |
| 时间窗口统计（stats） | q4, q15, q16, q17, q18, q19 | 累积/日历天/会话窗口 + count/min/max/avg/sum/top-N/last | 指标基线、行为统计、Top-N 异常排行、按天聚合看板 |
| 滑窗 / 跳窗 / 会话窗 | q5, q11 | HOP 跳窗、session 会话窗 | 滑动时间窗热门/突增检测、用户会话行为分析 |
| 序列检测 / 状态机（match） | q7 | match<key:dur> + top_ties | 攻击链序列匹配（暴力破解→提权→外联）、多步异常 |
| 多流 Join | q8, q9, q13, q20 | TUMBLE/asof/snapshot deferred join、展开关联 | 上下文富化（IP→资产、用户→部门）、关联告警归并、维表/快照关联 |
| 处理时间窗 | q12 | 每 bidder × 10s 处理时间窗计数 | 基于处理时间的限流/频控/去重 |

### 2.2 应用范围映射
- **实时数仓 / ETL**：q1/q2/q3/q10/q14/q21/q22 的投影、过滤、富化、落盘即典型流 ETL。
- **SIEM / 安全检测**：q7（序列匹配）、q4/q15–q19（行为统计与 Top-N）、q8/q9/q13/q20（上下文关联）对应检测规则、告警归并与实体行为分析。
- **IoT / 监控**：q5/q11（滑窗/会话突增）、q12（处理时间频控）对应指标异常与限流。
- **风控**：q18（每实体最后状态）、q19（Top-N）对应实时名单与排行风控。

> 各 Query 的具体流处理形态见 §4 性能 PK 主表「语义」列；q6 因架构性慢移出 `all` 套件（单跑保留），不计入主表。

## 3. 三方配置与度量口径（公平性核心）
| 维度 | warp-fusion | OSS Flink | VVR（实时计算 Flink） |
|---|---|---|---|
| 引擎版本 | warp-fusion（wp-reactor）v2.0.7 | 1.20.4 | vvr-11.5-jdk11-flink-1.20 |
| 数据规模 | **100,000,000**（PK 主表） | 100,000,000 | 100,000,000 |
| 计算资源 | Linux 8 核（与 VVR 同型号云服务器） | 3×ecs.g6a.xlarge = 12 vCPU / 48GiB | 8 CU ≈ 8 vCPU / 32 GiB（托管分布式集群，总资源=8C/32G，与 wfusion 同型号云服务器） |
| sink | 本地文件（blackhole 等价） | Blackhole | Blackhole |
| 指标 | EPS = Σn / (max emit − min start)（哨兵） | RPS = 输入量 / 用时 | RPS = 输入量 / 用时 |
| 来源 | 本仓库 `bench.sh` | 阿里白皮书 | 阿里白皮书 |

**度量口径说明**：
1. wfusion 的 **哨兵 EPS** 与白皮书的 **RPS** 思路同源——都是「消化（或处理）掉的记录数 ÷ 耗时」。哨兵机制从数据窗排空后取首尾时间戳，消除轮询粒度误差，短跑读数同样可信。
2. **规模已对齐（100M × 100M）**：本版 PK 主表与白皮书同为 1 亿条；**wfusion 与 VVR 使用相同型号云服务器**，残余不对等仅剩计算资源计量口径（8 核 vs 8CU，OSS 为 3×12vCPU），结论仍作量级参照。
3. 单轮数字存在 ±8% 相位噪声（bench 机 EPS 与 RSS 双峰相位强相关），结论已按 RSS 相位配对。
4. 引用 wfusion RSS 须标注 `parse_buffer_bytes`（默认 128MB；吞吐优先 2GB）。
5. **WFL 原语分工**：wfusion 规则以 **`match`**（序列/状态机检测）与 **`stats`**（列式统计聚合）两类一等原语表达；PK 边缘项 **q14**（vs VVR 最弱，Calculation 类、字符串处理重）与 **q18**（stats last，RSS 内存问题）为相对 VVR 的提升上限；均非 `match` 序列检测路径——q14 为 on-each 投影、q18 为 `stats` 聚合。
6. **VVR 相对 OSS 的平均倍数**：在 20 个有基线查询上，VVR RPS ÷ OSS RPS 的**几何平均 3.57×、算术平均 3.89×**，与白皮书公布的整体 **3.24×** 同量级（即 VVR 自身体现对开源 Flink 约 3~4× 的企业级优化）；wfusion 在此基础上进一步领先（见 §4）。

## 4. 性能 PK 主表（100M Linux · 2026-08-27 权威跑批 · v2.0.7）
> EPS/RPS 单位：条/秒。倍数 = wfusion EPS ÷ 对应基线 RPS。q13 白皮书未发布基线（重写后 O(1) snapshot join，已实测 8.17M），标 N/A。
| Query | 语义 | wfusion EPS | OSS RPS | VVR RPS | vs OSS | vs VVR | RSS(MB) |
|---|---|---:|---:|---:|---:|---:|---:|
| q1 | 无状态投影（货币换算 0.908×price + 过滤） | 16.85M | 1.75M | 4.38M | 9.62× | 3.85× | 4,185 |
| q2 | 选择（MOD(auction,123)=0 的 bid） | 31.08M | 1.93M | 6.57M | 16.13× | 4.73× | 4,285 |
| q3 | 按州过滤（IN OR/ID/CA） | 22.18M | 1.18M | 4.64M | 18.84× | 4.79× | 5,219 |
| q4 | 分类均价（累积窗口 deferred reduce + stats） | 3.74M | 0.18M | 0.64M | 20.69× | 5.87× | 18,459 |
| q5 | 热门新商品（HOP 10s/2s + top_ties(1)） | 2.00M | 0.27M | 0.28M | 7.31× | 7.15× | 13,546 |
| q7 | 时段最高出价（match<auction:10s> + top_ties） | 9.29M | 0.08M | 0.30M | 116.74× | 31.03× | 4,067 |
| q8 | 新用户 + 其拍卖（TUMBLE deferred join） | 28.97M | 1.25M | 3.34M | 23.12× | 8.67× | 5,264 |
| q9 | 中标出价（asof deferred reduce join） | 7.96M | 0.04M | 0.38M | 185.08× | 21.22× | 7,208 |
| q10 | 全量 bid 按时间分区落盘（每 bid 一行） | 17.30M | 0.53M | 1.95M | 32.82× | 8.85× | 4,245 |
| q11 | 用户会话统计（session 窗口 + 分片） | 10.85M | 0.24M | 0.69M | 44.36× | 15.84× | 4,085 |
| q12 | 每 bidder × 10s 处理时间窗计数 | 10.89M | 0.82M | 2.70M | 13.23× | 4.03× | 4,108 |
| q13 | 有界侧输入 join（snapshot join，重写后 O(1)） | 8.20M | — | — | N/A | N/A | 4,912 |
| q14 | 时间戳换算 + 价格过滤（Calculation） | 8.80M | 1.45M | 5.00M | 6.06× | 1.76× | 3,994 |
| q15 | 日历天出价统计（stats + 1d 桶） | 7.39M | 0.54M | 2.34M | 13.57× | 3.15× | 5,561 |
| q16 | 日历天渠道统计（stats + 1d 桶） | 5.92M | 0.11M | 0.30M | 54.36× | 19.96× | 7,470 |
| q17 | 日历天拍卖统计（stats + 1d 桶） | 8.36M | 0.97M | 3.69M | 8.60× | 2.26× | 18,068 |
| q18 | 每 (bidder,auction) 最后一条 bid（stats last） | 3.09M | 0.17M | 1.04M | 17.78× | 2.98× | 28,614 |
| q19 | 拍卖 Top-10 价格（stats<> top-N） | 7.86M | 0.17M | 1.05M | 46.08× | 7.48× | 6,620 |
| q20 | 展开 bid 关联 auction（snapshot join + where） | 17.27M | 0.07M | 0.43M | 231.49× | 39.98× | 8,046 |
| q21 | 附加 channel id（热通道映射 + cold url） | 13.67M | 0.79M | 2.52M | 17.38× | 5.43× | 4,219 |
| q22 | URL 目录投影 | 10.33M | 1.05M | 3.20M | 9.79× | 3.23× | 4,305 |

**结论**：vs OSS **6.06×~231.49× 全面领先**（20/20）；vs VVR **1.76×~39.98×，20/20 全部达 VVR**。
**平均倍数**：相对 OSS **几何 24.3× / 算术 44.7×**；相对 VVR **几何 6.8× / 算术 10.1×**（算术均值受 q20 等极端倍数抬升，几何均值更稳健）。

### 4.1 关于平均倍数：算术平均 vs 几何平均

报告对每个「相对倍数」同时给出**算术平均**与**几何平均**两个数（如相对 VVR：算术 10.1× / 几何 6.8×）。两者**来源完全相同**——都是 20 个有基线查询的逐查询倍数——只是求平均的方式不同：

- **算术平均**（10.1×）= 把 20 个倍数直接相加 ÷ 20，等权、「加法式」平均。
- **几何平均**（6.8×）= 20 个倍数相乘再开 20 次方，等价于「先取对数求算术平均、再取指数」，「乘法式」平均。

对任意正数都有 **算术平均 ≥ 几何平均**（AM–GM 不等式），故 10.1 > 6.8 是数学必然，关键在差多少。

**为何差约 1.5×**：倍数分布**严重右偏**——约一半查询只有 1.76×~7.15× 的典型倍数（落在几何均值 6.8× 下方），但 5 个「明星查询」倍数极高：**q7 31×、q9 21.2×、q11 15.8×、q16 20×、q20 40×**，形成长尾。算术平均给每个查询等权重，这几个离群值把均值从典型区硬拽到 10.1×；几何平均对极端值天然压缩，只反映**典型倍数**，落在 6.8×。

**为何报告以几何均值为头条**：对「提速倍数」这种**比率量**，几何均值更诚实——① 乘性对称（wfusion/VVR 几何 6.8× ⇄ VVR/wfusion 几何 1/6.8×≈0.147×，自洽；算术做不到）；② 倍数本就是乘法（「平均快 N 倍」意味着连乘而非连加）；③ 抗离群，不被少数明星查询绑架。

对照另两组也能印证这一规律：
- **VVR/OSS**：几何 3.57× / 算术 3.89×——差距很小，说明分布对称、无极端离群（与白皮书 3.24× 同量级）；
- **wfusion/OSS**：几何 24.3× / 算术 44.7×——差距巨大（1.8×），说明极度右偏（q20 达 231×），几何均值才不会被离群值带飞。

**怎么读这两个数**：几何均值 = 「随便挑一个查询，wfusion 大概比对手快多少倍」，**作结论头条**；算术均值 = 「把 20 个原始倍数无脑平摊」的结果，仅作**上限语境**（说明长尾明星查询把均值抬高了约 1.5×）。
边缘项：**q14 vs VVR 1.76×**（Calculation 类、字符串处理重，无状态投影，RSS 仅 3.9GB，为 vs VVR 最弱项）；q17 2.26×、q22 3.23×。**q13 白皮书无基线**。⚠ **q18 RSS 27.9GB** 为已知内存问题（100M 状态窗线性增长），建议跟进。

## 5. 规模缩放观测（同机 30M vs 100M）
> 同一台 Linux 8 核机、同一 v2.0.7，30M 与 100M 背靠背跑批。比值 = 100M EPS ÷ 30M EPS；<0.8 标记为规模退化（状态窗/状态随数据量增长）。
| Query | 30M EPS | 100M EPS | 100M/30M | 类型 |
|---|---:|---:|---:|---|
| q1 | 12.63M | 16.85M | 1.33× | 近线性/无退化 |
| q2 | 29.50M | 31.08M | 1.05× | 近线性/无退化 |
| q3 | 22.75M | 22.18M | 0.98× | 近线性/无退化 |
| q4 | 3.99M | 3.74M | 0.94× | 近线性/无退化 |
| q5 | 3.43M | 2.00M | 0.58× | 状态型退化 |
| q7 | 9.03M | 9.29M | 1.03× | 近线性/无退化 |
| q8 | 29.33M | 28.97M | 0.99× | 近线性/无退化 |
| q9 | 9.33M | 7.96M | 0.85× | 近线性/无退化 |
| q10 | 17.40M | 17.30M | 0.99× | 近线性/无退化 |
| q11 | 10.69M | 10.85M | 1.02× | 近线性/无退化 |
| q12 | 10.83M | 10.89M | 1.01× | 近线性/无退化 |
| q13 | 8.12M | 8.20M | 1.01× | 近线性/无退化 |
| q14 | 8.85M | 8.80M | 0.99× | 近线性/无退化 |
| q15 | 6.65M | 7.39M | 1.11× | 近线性/无退化 |
| q16 | 4.45M | 5.92M | 1.33× | 近线性/无退化 |
| q17 | 6.27M | 8.36M | 1.33× | 近线性/无退化 |
| q18 | 9.09M | 3.09M | 0.34× | 状态型退化 |
| q19 | 8.23M | 7.86M | 0.96× | 近线性/无退化 |
| q20 | 17.77M | 17.27M | 0.97× | 近线性/无退化 |
| q21 | 14.01M | 13.67M | 0.98× | 近线性/无退化 |
| q22 | 10.30M | 10.33M | 1.00× | 近线性/无退化 |

**结论**：无状态/轻量查询（q1/q2/q3/q7/q8/q10/q11/q12/q14/q15/q16/q19/q20/q21/q22）规模因子 0.94~1.24×，基本不随规模退化（部分 100M 略快属机器相位）；**状态型退化收敛为 q5 0.58×~0.67×、q18 0.34×**（q17 复跑 100M 反而更快，1.33×）——均由窗口状态随数据量增长驱动（RSS 翻倍）。q18 0.32× 伴随 30GB RSS，为已知内存问题。

## 6. 结果分析
### 6.1 按查询形态分类
- **无状态投影/过滤（q1/q2/q3/q8/q10/q14/q21/q22）**：EPS 5~30M，受单连接读链限制，吞吐最高。
- **窗口/聚合/join（q4/q5/q7/q9/q11/q12/q15–q18/q19/q20）**：中高 4~15M，受窗口状态与 join 维护成本主导。
- **状态重查询（q13/q18）**：q13 重写后 8.17M（snapshot join O(1)，取消 2d 窗口全保留）；q18 仅 3.09M 且 RSS 27.9GB（已知内存问题）。
### 6.2 边缘项：vs VVR 最弱是 q14（1.76×），非 q17
新版 100M 数据（晚间复跑、负载干净）下，**相对 VVR 最弱项是 q14（1.76×）**——Calculation 类（`0.908×price` 过滤 + `HOUR` 分型 + `count_char` UDF，on-each 无状态投影），字符串处理重、RSS 仅 3.9GB。q17（stats 1d 桶）实测 **2.26×**，非最弱项；每命中事件需构建 detail 字符串，属 Flink 语义固有成本；diag 墙表显示主墙 = **输出链**（+54.6ns）与规则段（+42.1ns），差距本质来自**引擎行式 cell 求值**，非查询写法问题；改进需进 wp-reactor 做列式字符串/过滤求值（通用能力，建议独立立项）。
### 6.3 已知问题与口径说明
- **q18 内存（27.9GB）🔴**：100M 状态窗线性增长，监视器曾测 ~60GB；建议内存归因跟进。
- **q5/q18 规模退化（0.58×~0.67×/0.34×）**：窗口状态随数据量增长，RSS 翻倍，属预期内状态型退化，非 bug。
- **q13 无白皮书基线**：snapshot join（重写后 O(1)）实测 8.17M，Flink 官方未发布对应档，不参与 vs 基线倍数计算。

## 7. 正确性与查询覆盖
- **覆盖**：查询集 22 条，自动化套件 **21/21 已完成 100M clean 跑批**（Q6 移出 `all`、单跑保留）。能力判定：**20 已有 · 1 待补强（Q12 处理时间窗）· 1 特殊口径（Q6 移出套件）**。
- **oracle 对拍（30M）**：Q4/Q9 **identical**（D4 保留 pin：join 目标窗字节上限驱逐不再静默丢行）；剩余已知差异：Q12 fixed+close 尾桶 known-diff、Q19 stats oracle 未接入（7 个 stats 规则组被 oracle 跳过）。
- **验证工具**：`wfgen verify-nexmark` 用真实 WFL 规则引擎对同一份确定性数据逐规则算出期望值，与引擎 EMIT 计数 git-diff 同款分层对拍（L1 哈希→L2 Myers→L3 明细）。

## 8. 资源消耗（RSS / CPU）
- **RSS 峰值（100M）**：无状态查询 4.0~5.6GB；状态重查询 **q18 27.9GB**、q17 17.6GB、q4 18.0GB、q9 7.0GB。q18 27.9GB 为已知内存问题（状态窗随数据量线性增长）。其余随消费速度饱和、30M 后基本有界。
- **CPU（活跃窗核占）**：无状态查询受单连接读链限制 ~100% avg（满核但单连接供给瓶颈）；重查询 avg 400%+（多核充分占用）。CPU 0% 旧口径假象已修复（亚秒突发在采样器首个差分前烧完），新口径下 0% 才可信。

## 9. 结论
warp-fusion 在 NEXMark 全查询上相对 OSS Flink **量级领先 6.06×–231.49×**，相对 VVR（实时计算 Flink）**全面达到且多数显著超出（1.76×–39.98×）**，正确性已达生产可用基线，21/21 套件查询端到端可运行（100M clean）。短板集中在：① q14/q17 类 Calculation / stats 的行式 cell 求值（通用引擎级优化）；② q18 状态窗内存（100M 27.9GB，已知问题）；③ Q12 处理时间窗（事件时间引擎固有，replay 下等价）。

## 附录 A：口径与引用
- **EPS 哨兵口径**：`EPS = Σn / (max emit_ns − min start_ns)`，数据窗排空后取首尾时间戳，无轮询粒度误差。
- **RSS 口径**：须标注 `parse_buffer_bytes`（默认 128MB，吞吐优先 2GB）。
- **A/B 纪律**：不限速（RATE=10000000）、同时段交错、按 RSS 相位配对、单轮 ±8% 噪声。
- **来源**：阿里云《性能白皮书（Nexmark 性能测试）》https://help.aliyun.com/zh/flink/realtime-flink/support/nexmark-performance-testing ；本仓库 `docs/OSS_VVR_BASELINE.md`、`docs/BENCH_RESULTS.md`、`docs/CAPABILITY_GAP_MATRIX.md`。
